const User =require("../models/user.model");
const register =async (req,res)=>{
 try {
    
 } catch (error) {
    
 }
}
const login = (req,res)=>{}
const logout = (req,res)=>{}


module.exports={
    register,
    login,
    logout
}